# launch the app 
==> go in part4 directory and type
python3 run.py
the backend run and is accessible by the adress : http://127.0.0.1:5000/api/v1/

==> To test the frond end use the liveserver extension of vscode and type the adress : 
http://localhost:5500/part4/login.html